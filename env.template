# Copy this file to `env.local` or `.env` (do not commit real secrets).

# === Google (Gemini API) ===
# Get your API key: https://aistudio.google.com/app/apikey
GOOGLE_AI_KEY=

# Model used to generate the JSON prediction (votes + questions).
# Latest models (2024-2025) - MUST include "models/" prefix:
#   - models/gemini-2.5-pro (latest, best quality, recommended)
#   - models/gemini-2.5-flash (latest, fastest, good quality)
#   - models/gemini-2.0-flash (stable fast model)
#   - models/gemini-1.5-pro (legacy, still works)
#   - models/gemini-1.5-flash (legacy, still works)
GOOGLE_PREDICT_MODEL=models/gemini-2.5-pro

# Optional embedding model for retrieving similar historical cases (better grounding).
# Default: models/text-embedding-004
# If unset, retrieval falls back to simple token overlap.
GOOGLE_EMBED_MODEL=models/text-embedding-004

# === App ===
APP_ENV=development
PORT=8000


